version: "3.9"
services:
  alloc:
    build:
      context: .
      dockerfile: docker/alloc/Dockerfile
    ports:
      - 4000:80
    links:
      - database:database
    depends_on:
      - "database"
  database:
    build:
      context: .
      dockerfile: docker/database/Dockerfile
    volumes:
      - database_data:/var/lib/mysql

volumes:
  database_data:
